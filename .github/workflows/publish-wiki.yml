name: Publish Wiki

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths: ['wiki/**']

# NOTE: pin Andrew-Chen-Wang/github-wiki-action to a commit SHA — it receives a
# contents:write token, so a repointed tag could push arbitrary commits.
permissions:
  contents: write

jobs:
  publish-wiki:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Upload Wiki
        uses: Andrew-Chen-Wang/github-wiki-action@v4
        with:
          path: wiki/
          token: ${{ secrets.GITHUB_TOKEN }}
